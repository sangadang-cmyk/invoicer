spring:
  application:
    name: invoicer
  cloud:
    aws:
      region:
        static: ap-southeast-1
management:
  endpoints:
    web:
      exposure:
        include: "*"
springdoc:
  swagger-ui:
    enabled: true
